version: "3.7"

services:
    app_proxy:
        environment:
            APP_HOST: miniharri-store-kavita_server_1
            APP_PORT: 5000

    server:
        image: jvmilazz0/kavita:latest
        init: true
        volumes:
            - ${APP_DATA_DIR}/data/config:/config