version: "3.7"

services:
    app_proxy:
        environment:
            APP_HOST: miniharri-store-komga_server_1
            APP_PORT: 25600

    server:
        image: gotson/komga
        init: true
        volumes:
            - ${APP_DATA_DIR}/data/config:/config
            - ${APP_DATA_DIR}/data:/data
